""" 
def random_id():
	numbers = '1234567890'
	letters = 'abcdefghijklmnopqrstuvwxyz'
	lettersUP = letters.upper()
	id = ''
	for i in range(0, 3):
		id += random.choice(numbers)
		id += random.choice(letters)
		id += random.choice(lettersUP)
	return id



	def get_img(url):
		self.url = url
		self.id = random_id()
		self.arq = f"{id}.jpg"
		self.response = requests.get(self.url)
		if self.response.status_code == 200:
			with open(self.arq, 'wb') as f:
				f.write(self.response.content)
	"""		